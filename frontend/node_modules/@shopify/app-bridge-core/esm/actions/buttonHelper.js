import { Action } from './Button/index.js';

function getSingleButton(action, button, subgroups, updateCb) {
    action.addChild(button, action.group, subgroups);
    action.subscribeToChild(button, Action.UPDATE, updateCb);
    return button.payload;
}

export { getSingleButton };
