import { actionWrapper } from '../helper.js';
import { Group } from '../types.js';

var Action;
(function (Action) {
    Action["REQUEST"] = "APP::SESSION_TOKEN::REQUEST";
    Action["RESPOND"] = "APP::SESSION_TOKEN::RESPOND";
})(Action || (Action = {}));
function request() {
    return actionWrapper({
        group: Group.SessionToken,
        type: Action.REQUEST,
    });
}
function respond(sessionToken) {
    return actionWrapper({
        payload: sessionToken,
        group: Group.SessionToken,
        type: Action.RESPOND,
    });
}

export { Action, request, respond };
