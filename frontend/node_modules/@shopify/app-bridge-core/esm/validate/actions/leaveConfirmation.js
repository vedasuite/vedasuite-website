import { Action } from '../../actions/LeaveConfirmation/index.js';
import { validate } from '../type-validate.js';
import { createActionValidator } from '../utils.js';

function validateAction(action) {
    const validator = createActionValidator(Action);
    return validate(action, validator);
}

export { Action, validateAction };
